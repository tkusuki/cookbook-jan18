<h1><%= @recipe_type.name %></h1>

<% if @recipe_type.recipes.empty? %>
  <p>Nenhuma receita encontrada para este tipo de receitas</p>
<% else %>

  <%= render partial: 'shared/recipes', locals: {recipes: @recipe_type.recipes} %>

<% end %>
